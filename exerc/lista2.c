//Guilherme Pereira Loredo - USP Sao Carlos 
//Codigo sobre Gerenciamentos de notas

#include<stdio.h>
#include <string.h>
#include<conio.h>


int int main() {
    int nalunos[];
    char nomeal,;
    int nualuno;
    FILE*dados;
    printf("Criar turma?");
    
    printf("Ver dados de alunos ja existentes?");
    fopen= ;
    printf("Digitir a quantidades de alunos na turma, com limite de 25");
    scanf("d", &nalunos);
    if(nalunos[i]= NULL){
        printf("Aluno nao cadatrado");
    }
     if(nalunos[i]= NULL)  {
     printf("Erro na abertura do arquivo!");
     return 1;
  }
    //nome do aluno
    printf("Informe o nome do Aluno");
     scanf("%s", nomeal);
  //usando fprintf para armazenar a string no arquivo
     fprintf(pont_arq, "%s", palavra);
  //usando fclose para fechar o arquivo
     fclose(pont_arq);



    return 0;
}